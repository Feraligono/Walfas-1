#ifndef __definitions
#define __definitions
struct svgFile {
        /*
         * This class is dedicated to holding the necessary
         * definitions and conversions from commands to 
         * necessary html
         */
        std::string id;
        std::string linear
};
#endif
